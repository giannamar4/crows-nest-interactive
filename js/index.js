//function that makes the banner alternate between the different games
function changeBanner() {
    let interval = setInterval(() => {
        console.log("hello");
    }, 1000);
}

// window.addEventListener("DOMContentLoaded",changeBanner);